# 0.3.24: bounded empty-Q15 reading panel

Branch `fix/empty-pdf-reading` follows 0.3.23. Only the shared `layout.css`
changes: precisely four direct Q15 missing-fact paragraphs share a warning
panel. Every paragraph, label, fact/status attribute and word remains intact.
Font size, line height, Jinja, translations, PDF engine and Word inputs are unchanged.

The selector requires the exact ordered expertise, hardware/software, repository
charges and costing gaps, all with missing status and no extra answer element.
Partial/negative answers, resource tables, nested and authored content do not match.
The existing bounded question/group page keep remains; arbitrary answers are not
made unbreakable. Removing repeated panel padding is not shrinking the text.

Frozen native-HTML preflight reduces empty Chinese PDF from four pages to three;
English remains four pages. Preflight is diagnostic, not a native DSW export.
Acceptance requires bilingual native empty/negative/full controls, short partial
budget and long missing-currency examples, with all fifteen questions and every
gap retained. Word body XML, styles and external links must stay unchanged.

The Chinese repo locks the exact source commit and verifies all 731 translations
and all prepared source files other than layout.css against 0.3.23. Historic
Q8/Q9 source-scope checks remain applicable to their own versions; ongoing Q8/Q9
Pandoc probes protect those behaviors when the current source-scope baseline moves.

Upstream stays 1.30.1 / 22d60aae4b63ee677477ac0c73097807284aaf9f. Upgrade work must
rerun the CSS engine and native outputs even if Git merges cleanly. Short-budget
prompt wrapping, broader prose quality, stock-worker Markdown tables and Microsoft
Word acceptance are separate open work; this is not a release or main merge.
