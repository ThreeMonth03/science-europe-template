{# Experimental profile: only the documented pilot notices are optional.
   This is not a whole-DMP completeness or submission approval mechanism. #}
{%- set output_profile = 'submission' -%}
{%- include 'src/index.html.j2' -%}
